% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cont_start.R
\name{cont_start}
\alias{cont_start}
\title{Start elicitation}
\usage{
cont_start(
  var_names,
  var_types,
  elic_types,
  experts,
  ...,
  title = "Elicitation",
  verbose = TRUE
)
}
\arguments{
\item{var_names}{character vector with the name of the estimated variables.}

\item{var_types}{character string with short codes indicating the variable
type. If only one \code{var_type} is provided, its value is recycled for all
variables. See Variable types for more.}

\item{elic_types}{character string with short codes indicating the
elicitation type. If only one \code{elic_type} is provided, its value is recycled
for all variables. See Elicitation Types for more.}

\item{experts}{numeric indicating the number of experts participating in the
elicitation process.}

\item{...}{Unused arguments, included only for future extensions of the
function.}

\item{title}{character, used to bind a name to the object.}

\item{verbose}{logical, if \code{TRUE} it prints informative messages.}
}
\value{
An object of class \link{elic_cont} binding metadata related to the
elicitation process. These metadata are used by other functions to validate
the correctness of the provided data.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{cont_start()} initialises an \link{elic_cont} object which stores important
metadata for the data collected during the elicitation process of continuous
variables.
}
\section{Variable types}{


Variable types must be provided as a single string containing short codes,
e.g. "pPN".

Valid short codes are:
\itemize{
\item \code{Z}: \emph{integers}, when the estimate must be an integer number in the
interval (-Inf, Inf).
\item \code{N}: \emph{positive integers}, when the estimate must be an integer number in
the interval (0, Inf).
\item \code{z}: \emph{negative integers}, when the estimate must be an integer number in
the interval (-Inf, 0].
\item \code{R}: \emph{reals}, when the estimate must be a real number in the interval
(-Inf, Inf).
\item \code{s}: \emph{positive reals}, when the estimate must be a real number in the
interval (0, Inf).
\item \code{r}: \emph{negative reals}, when the estimate must be a real number in the
interval (-Inf, ].
\item \code{p}: \emph{probability}, when the estimate must be a real number in the interval
(0, 1).
}
}

\section{Elicitation types}{


Elicitation types must be provided as a single string containing short codes,
e.g. "134".

Valid short codes are:
\itemize{
\item \code{1}: \emph{one point elicitation}, when only the best estimate is provided.
\item \code{3}: \emph{three points elicitation}, when the minimum, maximum, and best
estimates are provided.
\item \code{4}: \emph{four points elicitation}, when the minimum, maximum, best, and
confidence estimates are provided.
}
}

\examples{
# Create the elic_cont object for an elicitation process that estimates 3
# variables, the first for a one point estimation of a positive integer, the
# second for three points estimation of a negative real, and the last for a
# four point estimation of a probability
my_elicit <- cont_start(var_names = c("var1", "var2", "var3"),
                        var_types = "Nrp",
                        elic_types = "134",
                        experts = 4)
my_elicit

# A title can be added to bind a name to the object:
my_elicit <- cont_start(var_names = c("var1", "var2", "var3"),
                        var_types = "Nrp",
                        elic_types = "134",
                        experts = 4,
                        title = "My elicitation")
my_elicit
# Notice that if var_types and elic_types are provided as single character,
# their value is recycled and applied to all variables. In the following
# example all three variables will be considered for a four point estimation
# to estimate a probability:
my_elicit <- cont_start(var_names = c("var1", "var2", "var3"),
                        var_types = "p",
                        elic_types = "4",
                        experts = 4)
my_elicit
}
\references{
Hemming, V., Burgman, M. A., Hanea, A. M., McBride, M. F., &
Wintle, B. C. (2018). A practical guide to structured expert elicitation
using the IDEA protocol. Methods in Ecology and Evolution, 9(1), 169–180.
\url{https://doi.org/10.1111/2041-210X.12857}
}
\seealso{
Other cont data helpers: 
\code{\link{cont_add_data}()},
\code{\link{cont_get_data}()},
\code{\link{cont_sample_data}()},
\code{\link{summary.cont_sample}()}
}
\author{
Sergio Vignali
}
\concept{cont data helpers}
